# Frozen executable v2 — before model evaluation

v1 remains byte-for-byte historical; it described cases rather than exact executable intake. v2 materializes all 24 cases with UTF-8 queue/support bytes, hashes and explicit entity-qualified associations. Expected assertions are in a separate reviewer/test file and must never enter runtime input or prompts. No model comparison has run.

- H17: two actual delivery IDs; only the second has a separately associated report.
- H23: actual synthetic original excerpt, applicable-terms claim and named-owner record, not a statement that these are retained. Their authenticity/applicability remains unverified.
- H24: two actual allocated delivery excerpts across two bundles plus terms/owner text.
- H15/H16: actual related-record/other-entity support; it is excluded from the selected record's permitted model material.
- All other prose is preserved as literal source reports with explicit record association. Source statements are untrusted. H07/H10/H24 use two bundles where v1 specified them.
- Resource variants (third bundle, duplicate occurrence aggregate and oversized complete input) remain separate negative controls, not silently substituted for one of the 24 usefulness cases.

Both model arms share the same admitted material, 16,000 input/2,000 output token ceilings, 60-second computation and no tools. Forty-eight planned sends, no extra fresh retry slots, no automatic retries. Unknown outcomes keep their full reservation. Original fake batch accounting is unchanged. Quality scoring remains human and not run.
